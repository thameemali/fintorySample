name: fintory_sample
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  material_design_icons_flutter: 5.0.6595
  fluttertoast: ^8.0.8
  flutter_rating_bar: ^4.0.0
  counter_button: ^1.0.1
  shared_preferences: ^2.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/image/

  #fonts:
  #  - family: Dancing_Bold
  #    fonts:
  #      - asset: font/DancingScript-Bold.ttf

